{
  "name": "4.3-partials",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "nodemon --watch backend --exec babel-node backend/surver.js",
    "build": "rm -rf dist && babel backend -d dist && frontend && npm install && rpm run build",
    "serve": "node dist/serve.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-core": "^6.26.3",
    "body-parser": "^1.20.2",
    "braces": "^3.0.2",
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "yarn": "1.22.21"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0"
  }
}
